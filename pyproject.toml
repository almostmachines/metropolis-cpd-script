[project]
name = "metropolis-hastings-change-point-detection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib",
    "numpy",
    "plotly",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
